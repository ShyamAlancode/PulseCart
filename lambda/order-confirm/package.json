{
  "name": "pulsecart-order-confirm-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function to process DynamoDB Streams and send confirmation emails via SES",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.0.0",
    "@aws-sdk/util-dynamodb": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
